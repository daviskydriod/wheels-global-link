import { createFileRoute } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { PageIntro, VehicleGrid } from "@/components/marketplace";
import { Input } from "@/components/ui/input";
import { vehicles } from "@/lib/inventory";
import hero from "@/assets/awa-cars-category.jpg";

export const Route=createFileRoute("/cars")({head:()=>({meta:[{title:"Cars for Sale & Sourcing | AWA AUTO MALL"},{name:"description",content:"Explore representative vehicle inventory and request cars sourced from Guangzhou, China through AWA AUTO MALL."},{property:"og:title",content:"Cars for Sale & Sourcing | AWA AUTO MALL"},{property:"og:description",content:"Browse premium SUVs and sedans available through AWA AUTO MALL."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}],links:[{rel:"canonical",href:"/cars"}]}),component:CarsPage});
function CarsPage(){const [query,setQuery]=useState("");const [condition,setCondition]=useState("All");const shown=useMemo(()=>vehicles.filter(v=>(`${v.brand} ${v.model}`.toLowerCase().includes(query.toLowerCase()))&&(condition==="All"||v.condition===condition)),[query,condition]);return <><PageIntro eyebrow="Vehicle marketplace" title="Cars Sourced Around Your Needs" copy="Explore representative vehicles and tell us the exact make, model or specification you need." image={hero}/><section className="section-pad"><div className="container-shell"><div className="mb-10 grid gap-3 border border-border bg-secondary p-4 md:grid-cols-[1fr_240px]"><label><span className="sr-only">Search vehicles</span><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search make or model"/></label><label><span className="sr-only">Filter by condition</span><select value={condition} onChange={e=>setCondition(e.target.value)} className="h-9 w-full border border-input bg-background px-3 text-sm"><option>All</option><option>New</option><option>Pre-owned</option></select></label></div><VehicleGrid items={shown}/>{shown.length===0&&<p className="py-16 text-center text-muted-foreground">No vehicles match that search. Contact us and we can source it.</p>}</div></section></>}